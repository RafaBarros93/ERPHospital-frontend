import React from "react";
import { Container, Typography } from "@mui/material";

const Home: React.FC = () => {
  return (
    <Container>
      <Typography variant="h4">Bem-vindo à página Home</Typography>
    </Container>
  );
};

export default Home;
